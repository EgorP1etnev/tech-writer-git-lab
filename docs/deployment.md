# Deployment Guide

The platform can be deployed using Docker.

Basic deployment steps:

1. Install Docker
2. Clone the repository
3. Build the container image
4. Run the container

Further configuration may require environment variables.